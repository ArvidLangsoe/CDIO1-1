package boundary;

public interface ITUI {
	// Gets a string input from the user.
	public String getUserInput();
	// Shows the answer from the system to the user.
	public void showResponse(String response);
}
